# Environment files with secrets
.env
*.env
.env.*

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
venv/
env/

# IDE
.vscode/
.idea/

# OS
.DS_Store
._*
Thumbs.db

# Logs
*.log

# Never commit these
*secret*
*token*
*.key

# Node
node_modules/

# Local Claude config
.claude/

# Local-only — not shipped publicly
# CLAUDE.md = auth + XP architecture notes (don't expose for recon)
# login-test.html = auth pipeline debug page
# referral-card.html = static design mockup with hardcoded data; real per-
#                      user OG cards (when shipped) live at a different path
# db/ = supabase schema (reveals abuse-prevention triggers)
# xp/ = user data CSV backups + recovery scripts (contains real handles/IDs)
CLAUDE.md
login-test.html
referral-card.html
db/
xp/

# Snapshot/holder data (legacy from monorepo, kept defensively)
*_holders.json
*_holders.txt
*_holders.csv
snapshot.txt
*.xlsx

# Backup/copy files
*- Copy.*
* - Copy.*

# Local-only review/tooling notes (keep out of the public repo)
# fable.md = full-site security/perf review; a map of live weaknesses
# scripts/transak_local_session.* = local dev proxy for the parked wallet
#                                   Fund flow (API key / IP-allowlist notes)
fable.md
scripts/transak_local_session.py
scripts/transak_local_session.ps1
